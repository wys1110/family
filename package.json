{
  "name": "family",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "check": "node --check app.js && node --check config.js && node --check baby-ai.js && node --check baby-ai-core.js && tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^20.19.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
